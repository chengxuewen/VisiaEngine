# visia-core 行为契约（SDD）

> 条款格式：`## CORE-NN: 名称`，正文 = 输入/输出/错误语义一段话。测试注释携带 `// spec: CORE-NN`。
> 双向追溯门禁：`scripts/spec-trace.sh`（有规无测 / 有测无规 = 红）。
> 坐标重基（RTC）相关测试留 P1 裁定后追加（architecture.md 未决点），不预写。

（S0 空壳。CORE-01..10 随 S1 片填充。）
